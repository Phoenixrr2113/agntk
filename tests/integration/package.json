{
  "name": "integration-tests",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:tools": "vitest run src/tools/",
    "test:pipeline": "vitest run src/pipeline/",
    "test:cli": "vitest run src/cli/",
    "test:regression": "vitest run src/tools/ src/pipeline/ src/cli/"
  },
  "devDependencies": {
    "@agntk/core": "workspace:*",
    "@agntk/server": "workspace:*",
    "@agntk/client": "workspace:*",
    "ai": "6.0.177",
    "zod": "^4.4.3",
    "vitest": "^4.1.6",
    "typescript": "^6.0.3"
  }
}
